% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AUCContour.R
\name{coxAUC}
\alias{coxAUC}
\title{Time-dependent AUC contour for Cox proportional hazards models}
\usage{
coxAUC(
  data,
  model,
  contCov,
  statusVar,
  timeVar,
  contCovName = NULL,
  eval_times = NULL,
  nCovEval = 30,
  nCores = 2,
  drawHistogram = TRUE
)
}
\arguments{
\item{data}{A data.frame containing at least \code{timeVar}, \code{statusVar},
and \code{contCov}.}

\item{model}{A fitted \code{\link[survival]{coxph}} model.}

\item{contCov}{Character, name of the continuous covariate.}

\item{statusVar}{Character, event indicator (0/1).}

\item{timeVar}{Character, follow-up time variable.}

\item{contCovName}{Optional y-axis label. Defaults to `contCov`.}

\item{eval_times}{Optional numeric vector of evaluation times. If missing,
extracted from \code{survival::survfit(model)}.}

\item{nCovEval}{Integer, number of thresholds between 5–95\% quantiles.}

\item{nCores}{Integer, CPU cores for parallelization.}

\item{drawHistogram}{Logical; if TRUE attach histogram of \code{contCov}.}
}
\value{
A Plotly contour heatmap (with or without histogram), showing AUC as a
function of time and the \code{contCov} threshold.
}
\description{
Computes a time-dependent IPCW-adjusted AUC across follow-up time (x-axis)
and a sequence of thresholds of a continuous covariate (y-axis) for a fitted
Cox proportional hazards model.
}
\details{
For each threshold value \eqn{c}, a binary variable \eqn{I(\mathrm{contCov} > c)}
is created by anchoring \code{contCov} at its 5–95\% quantiles and a Cox model
is refit. IPCW-adjusted time-dependent AUC is then computed via \pkg{riskRegression}.
\pkg{riskRegression}.
}
\examples{
\dontrun{
vet <- survival::veteran
fit <- survival::coxph(Surv(time, status) ~ age, data = vet)
p <- coxAUC(vet, fit, contCov = "age",
               statusVar = "status", timeVar = "time",
               nCovEval = 20, nCores = 2, drawHistogram = TRUE)
}

}
\seealso{
\code{\link{AUCContour}}, \code{\link{rsfAUC}}, \code{\link{xgbAUC}}
}
