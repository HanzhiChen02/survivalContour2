% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CIndexContour.R
\name{CIndexContour}
\alias{CIndexContour}
\title{Time-dependent C-index contour (dispatcher for Cox / RSF / XGB)}
\usage{
CIndexContour(
  data,
  model,
  contCov,
  statusVar = NULL,
  timeVar = NULL,
  contCovName = NULL,
  nCovEval = 30,
  nCores = 4,
  eval_times = NULL,
  drawHistogram = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} containing variables used in the fitted model.}

\item{model}{A fitted survival model: one of \code{coxph}, \code{rfsrc},
or \code{xgb.Booster}.}

\item{contCov}{Character string; name of the continuous covariate used
to define thresholds on the y-axis.}

\item{statusVar}{Character string; name of the event indicator (0/1).}

\item{timeVar}{Character string; name of the follow-up time variable.}

\item{contCovName}{Optional label for the y-axis. Defaults to \code{contCov}.}

\item{nCovEval}{Integer; number of thresholds between the 5\% and 95\%
quantiles of \code{contCov}.}

\item{nCores}{Integer; number of CPU cores passed to the underlying
C-index backends (for parallelization, where applicable).}

\item{eval_times}{Optional numeric vector of evaluation time points on
the x-axis. If \code{NULL}, reasonable defaults are chosen within the
backend functions.}

\item{drawHistogram}{Logical; if \code{TRUE}, a histogram of \code{contCov}
is attached to the right of the contour plot.}
}
\value{
A Plotly figure. Depending on \code{drawHistogram}, this is either
a single contour heatmap or a side-by-side subplot combining the
contour with a histogram of the continuous covariate.
}
\description{
Generic wrapper for computing a time-dependent C-index surface over
follow-up time (x-axis) and a continuous covariate threshold (y-axis).
It automatically dispatches to the appropriate backend based on the
class of \code{model}:

\itemize{
  \item \code{coxph}: uses \code{\link{coxCIndex}}
  \item \code{rfsrc}: uses \code{\link{rsfCIndex}}
  \item \code{xgb.Booster}: uses \code{\link{xgbCIndex}}
}
}
\seealso{
\code{\link{coxCIndex}}, \code{\link{rsfCIndex}}, \code{\link{xgbCIndex}}
}
