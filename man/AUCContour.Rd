% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AUCContour.R
\name{AUCContour}
\alias{AUCContour}
\title{Time-dependent AUC contour (dispatcher for Cox / RSF / XGB)}
\usage{
AUCContour(
  data,
  model,
  contCov,
  statusVar,
  timeVar,
  contCovName = NULL,
  eval_times = NULL,
  nCovEval = 30,
  nCores = 2,
  drawHistogram = TRUE
)
}
\arguments{
\item{data}{A data.frame containing variables used in the fitted model,
including \code{timeVar}, \code{statusVar} and \code{contCov}.}

\item{model}{A fitted survival model of class one of \code{coxph}, \code{rfsrc},
or \code{xgb.Booster}.}

\item{contCov}{Character string; name of the continuous covariate used for
constructing thresholds.}

\item{statusVar}{Character string; event indicator (0/1).}

\item{timeVar}{Character string; follow-up time variable.}

\item{contCovName}{Optional y-axis label for the contour plot. Defaults
to \code{contCov}.}

\item{eval_times}{Optional numeric vector of evaluation time points. If
\code{NULL}, suitable times are computed internally by the model-specific
methods.}

\item{nCovEval}{Integer; number of thresholds between the 5--95\% quantiles
of \code{contCov}.}

\item{nCores}{Integer; number of CPU cores used by the underlying methods.}

\item{drawHistogram}{Logical; if \code{TRUE}, a marginal histogram of
\code{contCov} is displayed to the right of the contour plot.}
}
\value{
A \pkg{plotly} object containing either a single contour heatmap or a
subplot of the contour and the marginal histogram.
}
\description{
High-level wrapper that computes a time-dependent AUC surface over follow-up
time (x-axis) and thresholds of a continuous covariate (y-axis) using a
fitted Cox model, Random Survival Forest, or XGBoost AFT model.

\itemize{
  \item \code{coxph}: uses \code{\link{coxAUC}}
  \item \code{rfsrc}: uses \code{\link{rsfAUC}}
  \item \code{xgb.Booster}: uses \code{\link{xgbAUC}}
}
}
\seealso{
\code{\link{coxAUC}}, \code{\link{rsfAUC}}, \code{\link{xgbAUC}}
}
