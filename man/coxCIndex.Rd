% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CIndexContour.R
\name{coxCIndex}
\alias{coxCIndex}
\title{Time-dependent C-index contour for Cox proportional hazards models}
\usage{
coxCIndex(
  data,
  model,
  contCov,
  statusVar = NULL,
  timeVar = NULL,
  contCovName = NULL,
  nCovEval = 30,
  nCores = 4,
  eval_times = NULL,
  drawHistogram = TRUE
)
}
\arguments{
\item{data}{A data.frame containing at least \code{timeVar}, \code{statusVar},
and \code{contCov}.}

\item{model}{A fitted \code{\link[survival]{coxph}} model.}

\item{contCov}{Character, name of the continuous covariate.}

\item{statusVar}{Character, event indicator (0/1).}

\item{timeVar}{Character, follow-up time variable.}

\item{contCovName}{Optional y-axis label. Defaults to `contCov`.}

\item{nCovEval}{Integer, number of thresholds between 5–95\% quantiles.}

\item{nCores}{Integer, CPU cores for parallelization.}

\item{eval_times}{Optional numeric vector of evaluation times. If missing,
extracted from \code{survival::survfit(model)}.}

\item{drawHistogram}{Logical; if TRUE attach histogram of \code{contCov}.}
}
\value{
A Plotly contour heatmap (with or without histogram).
}
\description{
Computes a time-dependent IPCW-adjusted C-index across follow-up time (x-axis)
and a sequence of thresholds of a continuous covariate (y-axis).
Returns a Plotly contour plot, optionally with an attached histogram.
}
\details{
For each threshold value \eqn{c}, a binary variable \eqn{I(contCov > c)} is created
and a Cox model is refit. IPCW-adjusted C-index is computed via \pkg{pec} /
\pkg{riskRegression}.
}
\examples{
\dontrun{
vet <- survival::veteran
fit <- survival::coxph(Surv(time, status) ~ age, data = vet)
p <- coxCIndex(vet, fit, contCov = "age",
               statusVar = "status", timeVar = "time")
}

}
\seealso{
\code{\link{CIndexContour}}, \code{\link{rsfCIndex}}, \code{\link{xgbCIndex}}
}
